///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Mesen2 GBA Core Plugin - Wrapper around Mesen2 Game Boy Advance emulator
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#include "mesen2_shared.h"
#include "Core/GBA/GbaConsole.h"

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// GBA system configuration

static const Mesen2SystemInfo s_gba_info = {
    .emu_name = "Mesen2",
    .system_name = "Game Boy Advance",
    .extensions = "gba",
    .requires_bios = true,
    .default_width = 240,
    .default_height = 160,
    .default_fps = 59.7275f,
    .cpu_type = CpuType::Gba,
    .visible_x = 0,
    .visible_y = 0,
    .visible_width = 240,
    .visible_height = 160,
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_get_info(RpEmuInfo* info) {
    info->emu_name = s_gba_info.emu_name;
    info->emu_version = "2.0";
    info->system_name = s_gba_info.system_name;
    info->supported_extensions = s_gba_info.extensions;
    info->requires_bios = s_gba_info.requires_bios;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void* mesen2_gba_create(FlArena* arena) {
    return mesen2_create_core(arena, s_gba_info, "GBA");
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_destroy(void* ci) {
    mesen2_destroy_core((Mesen2CoreBase*)ci, "GBA");
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static bool mesen2_gba_mount_media(void* ci, const char* path) {
    return mesen2_mount_media((Mesen2CoreBase*)ci, path);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_unmount_media(void* ci) {
    mesen2_unmount_media((Mesen2CoreBase*)ci);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_run_frame(void* ci, RpEmuFrameContext* ctx) {
    mesen2_run_frame((Mesen2CoreBase*)ci, ctx);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_reset(void* ci) {
    mesen2_reset((Mesen2CoreBase*)ci);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_hard_reset(void* ci) {
    mesen2_hard_reset((Mesen2CoreBase*)ci);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_get_video_spec(void* ci, RpVideoSpec* spec) {
    mesen2_get_video_spec((Mesen2CoreBase*)ci, spec);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_get_audio_spec(void* ci, RpAudioSpec* spec) {
    (void)ci;
    mesen2_get_audio_spec(spec);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static RpEmuLifecycleState mesen2_gba_get_state(void* ci) {
    return mesen2_get_state((Mesen2CoreBase*)ci);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static size_t mesen2_gba_serialize(void* ci, void* b, size_t bs) {
    (void)ci;
    (void)b;
    (void)bs;
    return 0;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static bool mesen2_gba_deserialize(void* ci, const void* b, size_t bs) {
    (void)ci;
    (void)b;
    (void)bs;
    return false;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_set_config_int(void* ci, FlString k, int32_t v) {
    (void)ci;
    (void)k;
    (void)v;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_set_config_float(void* ci, FlString k, float v) {
    (void)ci;
    (void)k;
    (void)v;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_set_config_bool(void* ci, FlString k, bool v) {
    (void)ci;
    (void)k;
    (void)v;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gba_set_config_string(void* ci, FlString k, FlString v) {
    (void)ci;
    (void)k;
    (void)v;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static FlString mesen2_gba_get_config_string(void* ci, FlString k) {
    (void)ci;
    (void)k;
    return string_empty();
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static RpEmuAPI s_mesen2_gba_api = {
    .get_info = mesen2_gba_get_info,
    .create = mesen2_gba_create,
    .destroy = mesen2_gba_destroy,
    .mount_media = mesen2_gba_mount_media,
    .unmount_media = mesen2_gba_unmount_media,
    .run_frame = mesen2_gba_run_frame,
    .reset = mesen2_gba_reset,
    .hard_reset = mesen2_gba_hard_reset,
    .get_video_spec = mesen2_gba_get_video_spec,
    .get_audio_spec = mesen2_gba_get_audio_spec,
    .get_state = mesen2_gba_get_state,
    .copy_audio = nullptr,
    .serialize = mesen2_gba_serialize,
    .deserialize = mesen2_gba_deserialize,
    .set_config_bitmask = nullptr,
    .set_config_int = mesen2_gba_set_config_int,
    .set_config_float = mesen2_gba_set_config_float,
    .set_config_bool = mesen2_gba_set_config_bool,
    .set_config_string = mesen2_gba_set_config_string,
    .get_config_string = mesen2_gba_get_config_string,
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

extern "C" RP_EMU_EXPORT const RpEmuAPI* rp_emu_plugin_get(void) {
    return &s_mesen2_gba_api;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// ABI version export. The host resolves this before it reads the vtable above and refuses the
// plugin when the answer is not the version it was built for.

RP_EMU_PLUGIN_ABI_VERSION_EXPORT()
