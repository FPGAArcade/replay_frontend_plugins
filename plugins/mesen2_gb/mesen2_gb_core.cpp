///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Mesen2 Game Boy Core Plugin - Wrapper around Mesen2 Game Boy/Color emulator
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#include "mesen2_shared.h"
#include "Core/Gameboy/Gameboy.h"

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Game Boy system configuration

static const Mesen2SystemInfo s_gb_info = {
    .emu_name = "Mesen2",
    .system_name = "Game Boy / Game Boy Color",
    .extensions = "gb|gbc|gbs",
    .requires_bios = false,
    .default_width = 160,
    .default_height = 144,
    .default_fps = 59.7275f,
    .cpu_type = CpuType::Gameboy,
    .visible_x = 0,
    .visible_y = 0,
    .visible_width = 160,
    .visible_height = 144,
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_get_info(RpEmuInfo* info) {
    info->emu_name = s_gb_info.emu_name;
    info->emu_version = "2.0";
    info->system_name = s_gb_info.system_name;
    info->supported_extensions = s_gb_info.extensions;
    info->requires_bios = s_gb_info.requires_bios;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void* mesen2_gb_create(FlArena* arena) {
    return mesen2_create_core(arena, s_gb_info, "Game Boy");
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_destroy(void* ci) {
    mesen2_destroy_core((Mesen2CoreBase*)ci, "Game Boy");
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static bool mesen2_gb_mount_media(void* ci, const char* path) {
    return mesen2_mount_media((Mesen2CoreBase*)ci, path);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_unmount_media(void* ci) {
    mesen2_unmount_media((Mesen2CoreBase*)ci);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_run_frame(void* ci, RpEmuFrameContext* ctx) {
    mesen2_run_frame((Mesen2CoreBase*)ci, ctx);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_reset(void* ci) {
    mesen2_reset((Mesen2CoreBase*)ci);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_hard_reset(void* ci) {
    mesen2_hard_reset((Mesen2CoreBase*)ci);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_get_video_spec(void* ci, RpVideoSpec* spec) {
    mesen2_get_video_spec((Mesen2CoreBase*)ci, spec);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_get_audio_spec(void* ci, RpAudioSpec* spec) {
    (void)ci;
    mesen2_get_audio_spec(spec);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static RpEmuLifecycleState mesen2_gb_get_state(void* ci) {
    return mesen2_get_state((Mesen2CoreBase*)ci);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static size_t mesen2_gb_serialize(void* ci, void* b, size_t bs) {
    (void)ci;
    (void)b;
    (void)bs;
    return 0;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static bool mesen2_gb_deserialize(void* ci, const void* b, size_t bs) {
    (void)ci;
    (void)b;
    (void)bs;
    return false;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_set_config_int(void* ci, FlString k, int32_t v) {
    (void)ci;
    (void)k;
    (void)v;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_set_config_float(void* ci, FlString k, float v) {
    (void)ci;
    (void)k;
    (void)v;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_set_config_bool(void* ci, FlString k, bool v) {
    (void)ci;
    (void)k;
    (void)v;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static void mesen2_gb_set_config_string(void* ci, FlString k, FlString v) {
    (void)ci;
    (void)k;
    (void)v;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static FlString mesen2_gb_get_config_string(void* ci, FlString k) {
    (void)ci;
    (void)k;
    return string_empty();
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static RpEmuAPI s_mesen2_gb_api = {
    .get_info = mesen2_gb_get_info,
    .create = mesen2_gb_create,
    .destroy = mesen2_gb_destroy,
    .mount_media = mesen2_gb_mount_media,
    .unmount_media = mesen2_gb_unmount_media,
    .run_frame = mesen2_gb_run_frame,
    .reset = mesen2_gb_reset,
    .hard_reset = mesen2_gb_hard_reset,
    .get_video_spec = mesen2_gb_get_video_spec,
    .get_audio_spec = mesen2_gb_get_audio_spec,
    .get_state = mesen2_gb_get_state,
    .copy_audio = nullptr,
    .serialize = mesen2_gb_serialize,
    .deserialize = mesen2_gb_deserialize,
    .set_config_bitmask = nullptr,
    .set_config_int = mesen2_gb_set_config_int,
    .set_config_float = mesen2_gb_set_config_float,
    .set_config_bool = mesen2_gb_set_config_bool,
    .set_config_string = mesen2_gb_set_config_string,
    .get_config_string = mesen2_gb_get_config_string,
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

extern "C" RP_EMU_EXPORT const RpEmuAPI* rp_emu_plugin_get(void) {
    return &s_mesen2_gb_api;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// ABI version export. The host resolves this before it reads the vtable above and refuses the
// plugin when the answer is not the version it was built for.

RP_EMU_PLUGIN_ABI_VERSION_EXPORT()
